# fill up 3x3  matrix with values such that each row and column and diagonal 
# sums up to same value


def check_diagonal(grid, row, col, target):



def check_horizontal(grid, row, target):
    return (grid[row][0] + grid[row][1] + grid[row][2]) == target

def check_vertical(grid, col, target):
    return (grid[0][col] + grid[1][col] + grid[2][col]) == target

def generate_magic_matrix(grid):
    for target range()
